import React from 'react'
import GuestContainer from './Guest'

const PageContainer = () => {
  return (
    <div className="app-layout">
      <GuestContainer />
    </div>
  )
}

export default PageContainer
